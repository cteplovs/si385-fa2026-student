[project]
name = "si385-2026"
version = "0.1.0"
description = "SI 385 — Data Exploration — Fall 2026"
requires-python = ">=3.11"
dependencies = [
    "marimo",
    "pandas",
    "numpy",
    "matplotlib",
    "seaborn",
]
